STRICT RULES:
- Use ONLY the provided checklist content, variables and locators
- DO NOT invent features, behaviors, or assumptions
- DO NOT add functionality not explicitly described
- Cover both positive and negative scenarios when applicable
- Include edge cases if they are implied by the checklist content
- Base test only on provided variables and given locators
- Do not speculate on the behavior, if expected behavior would be an error message, do not provide a specific expected message, but just check that error message box appears based on locator as expected

STRUCTURE REQUIREMENTS:
- Each scenario must have:
  - "name" (clear and concise)
  - "steps": list of steps
- Each step must include:
  - "step"
  - "expected"

OUTPUT FORMAT (STRICT JSON ONLY):
{
  "scenarios": [
    {
      "name": "Valid login",
      "steps": [
        {
          "step": "Enter correct username",
          "expected": "Username is accepted"
        }
      ]
    }
  ]
}

CHECKLIST CONTENT:
{{CHECKLIST}}

TEST VARIABLES:
{{VARIABLES}}

PAGE_LOCATORS:
{{PAGE_LOCATORS}}